# Zroky Backend Build Log

This file records major implementation changes in chronological order.

- 2026-04-25: Completed launch-gate modules 14-18 across backend and dashboard: fix adoption viewed-vs-resolved tracking from audit logs, feedback loop thumbs-down percentage by diagnosis category, SDK `0.1.x` compatibility regression tests for backend `v1+` ingest routes, pricing validation interview evidence with >=5 lock gate and lock immutability enforcement, rollback drill pass gating with deploy/rollback/failure-simulation requirements, and settings/home UI wiring; validated with backend pytest suites and dashboard lint.
- 2026-04-25: Implemented DB-backed LOOP_DETECTED repeat counting in worker orchestration using bounded 90-second queries on indexed first-class columns (`tenant_id`, `agent_name`, `prompt_fingerprint`, `created_at`), added `diagnosis_jobs.agent_name` + composite index via Alembic `0011`, persisted `agent_name`/`prompt_fingerprint` across ingest/submit/onboarding write paths, and added worker tests for bounded window filtering + threshold-triggered loop detection.
- 2026-04-25: Implemented production-grade SDK prompt fingerprinting (`normalize_text/messages/tools` + SHA-256 fail-safe hash), wired fingerprint capture into both `zroky.call` and `zroky.record`, passed fingerprint through backend ingest schema/payload, and added deterministic regression coverage for number/whitespace normalization plus ingest persistence assertions.
- 2026-04-25: Added first-class `diagnosis_jobs.prompt_fingerprint` persistence with Alembic migration `0010` + tenant/prompt/created index, updated ingest write-path to store the field directly, and added DB-level grouping tests proving five same-signature calls are query-groupable for LOOP_DETECTED readiness.
- 2026-04-21: Created backend scaffold (FastAPI, SQLAlchemy, Alembic, Redis, Celery, Docker/Railway artifacts, baseline tests).
- 2026-04-21: Added diagnosis submit/status flow and worker status persistence.
- 2026-04-21: Added tenant context enforcement and mismatch checks.
- 2026-04-21: Added project provisioning and API key issue/list/revoke flows.
- 2026-04-21: Added API key based project resolution for diagnosis routes.
- 2026-04-21: Standardized API key defaults (name: Zroky API, format: zroky_api_live_*).
- 2026-04-21: Added hardening toggles (`ALLOW_PROJECT_HEADER_CONTEXT`, `REQUIRE_PROVISIONING_TOKEN`) and coverage tests.
- 2026-04-21: Added strict production runtime guard, production env template, startup preflight validation, and production config tests.
- 2026-04-21: Added Railway go-live checklist with click-by-click deployment sequence, env matrix, smoke tests, and rollback drill steps.
- 2026-04-22: Added JWT identity integration (claims-based project scoping, admin-role provisioning fallback), auth env contract updates, and JWT negative/positive tests.
- 2026-04-22: Added users and project_memberships migration/model layer, project membership provisioning APIs, owner bootstrap membership, and optional DB-backed JWT membership enforcement.
- 2026-04-22: Added role-based authorization matrix (diagnosis member/viewer gates and project-scoped admin/owner controls) plus PostgreSQL RLS policy migration for diagnosis_jobs tenant isolation.
- 2026-04-22: Added Postgres-only RLS integration tests and GitHub Actions dual-run CI workflow (`sqlite-fast` + `postgres-security`) for security regression coverage.
- 2026-04-22: Hardened Alembic env URL resolution to honor `DATABASE_URL` directly and force PostgreSQL migration `search_path` to `public` for consistent tenant table placement.
- 2026-04-22: Stabilized Postgres RLS test fixture with idempotent public `diagnosis_jobs` + policy bootstrap and non-superuser role execution to avoid superuser RLS bypass.
- 2026-04-23: Added lint/type quality gate (`ruff` + `mypy`) in CI via new `lint-type` job, introduced `requirements-dev.txt` + `mypy.ini`, and fixed mypy issues in DB session and JWT identity modules.
- 2026-04-23: Added script-first DB operator utilities (`scripts/db_inspect.py`, `scripts/run_postgres_rls_suite.py`) and README runbook updates to eliminate shell one-liner quoting failures.
- 2026-04-23: Implemented Step 06 diagnosis engine contracts with deterministic 5-rule evaluation (`TOKEN_OVERFLOW`, `RATE_LIMIT`, `AUTH_FAILURE`, `LOOP_DETECTED`, `COST_SPIKE`), overflow subtype handling, provider degradation context, loop suppression checks, cost warm-up gate + baseline metadata, blast-radius output, and fast/pattern queue split task scaffolding with unit test coverage.
- 2026-04-23: Implemented Step 07 diagnosis feedback and share-link support with new feedback/share-token tables and migration (`0005`), secure hashed share token issuance (`/v1/diagnosis/{id}/share`), public read-only share retrieval with expiry/revocation enforcement (`/v1/diagnosis/share/{token}`), project-admin share token revoke/list APIs, and regression tests for feedback/share/create/read/expire/revoke role controls.
- 2026-04-23: Implemented Step 08 observability baseline with structured JSON logging (`LOG_FORMAT`), request correlation middleware (`X-Request-Id`), Prometheus metrics endpoint (`/metrics`) with optional token protection, HTTP and diagnosis job/category metrics instrumentation, and health tests for request-id + metrics exposure/auth behavior.
- 2026-04-23: Advanced Step 09 CI hardening with workflow concurrency + minimal permissions, optional non-blocking security audit lane (`bandit` + `pip-audit` artifacts), and branch-protection automation tooling (`scripts/configure-branch-protection.ps1`, `BRANCH_PROTECTION.md`) for enforcing required status checks.
- 2026-04-23: Removed accidental local `.git` initializations in `zroky-backend`; final Step 09 branch-protection enforcement remains pending external GitHub repo context and authenticated `gh` session.
- 2026-04-23: Completed Step 09 branch-protection enforcement on `zrokylayers/intel` `main` with required checks (`lint-type`, `sqlite-fast`, `postgres-security`) and fixed PowerShell JSON payload serialization in `scripts/configure-branch-protection.ps1` by writing UTF-8 without BOM to avoid GitHub API parse errors.
- 2026-04-23: Added `scripts/verify-branch-protection.ps1` for machine-checking branch protection drift (required checks and merge safety flags) and documented verification command in `BRANCH_PROTECTION.md`.
- 2026-04-23: Added scheduled/manual GitHub Actions drift audit workflow (`.github/workflows/zroky-branch-protection-audit.yml`) to run branch-protection verification against the target repo using `BRANCH_PROTECTION_AUDIT_TOKEN`.
- 2026-04-23: Fixed audit workflow runtime auth to use `GH_TOKEN=${{ secrets.BRANCH_PROTECTION_AUDIT_TOKEN }}` directly (removed `gh auth login` step), deployed to `zrokylayers/intel`, and validated successful run (`24845675651`) with required checks still enforced.
- 2026-04-23: Added `scripts/railway_smoke_check.py` to automate Railway go-live smoke validation (liveness, readiness, provisioning guard, and authenticated provisioning flow), and wired usage docs into `RAILWAY_GO_LIVE_CHECKLIST.md` and `README.md`.
- 2026-04-24: Expanded PostgreSQL FORCE RLS coverage to additional tenant tables via Alembic `0009` (`diagnosis_feedback`, `diagnosis_share_tokens`, `diagnosis_fix_watches`, `project_alerts`, `project_dashboard_configs`), added worker/session tenant-context re-apply hook for multi-transaction safety, and extended Postgres RLS tests with `project_alerts` cross-tenant read/write negative coverage.
- 2026-04-24: Implemented worker durability hardening with configurable diagnosis retry backoff settings (`DIAGNOSIS_TASK_*`), persisted `retrying` and terminal `dead_lettered` job states, and new worker regression tests; added provider-status timeout/cache fallback resolver (`PROVIDER_STATUS_*` settings) wired into RATE_LIMIT diagnosis evidence path with cache-first fallback tests.
- 2026-04-24: Replaced synthetic-only provider test verification with real status-endpoint connectivity checks (`verify_provider_connection`) using timeout + cache fallback + explicit failure reason propagation to Settings/Providers APIs, and added deterministic integration/unit tests for both verified and failed paths.
- 2026-04-24: Added credentialed provider handshake verification for OpenAI/Anthropic (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`) that performs live auth probes before status-endpoint fallback, fails fast on rejected credentials, and includes regression tests for credentialed success/failure paths.
- 2026-04-23: Delivered dashboard backend phase 0 APIs: added Calls (`/v1/calls` + detail), Analytics (`/v1/analytics/*` summary/health/cost/budget), Alerts (`/v1/alerts` list/detail/actions/channel-test), Onboarding synthetic failure trigger (`/v1/onboarding/trigger-test-failure`), and Settings APIs (project/pii/retention/notifications/provider verification), plus new dashboard persistence tables (`project_alerts`, `project_dashboard_configs`) in Alembic `0006`, worker-side alert materialization, and regression tests in `tests/test_dashboard_phase0.py`.
- 2026-04-23: Started dashboard frontend phase 1 in new `zroky-dashboard` Next.js app with route-complete scaffold (Home, Calls, Call Detail, Cost, Alerts, Settings, Auth Login/Register, Onboarding), responsive shell + monochrome design tokens, typed API client mapped to phase-0 backend endpoints, and server proxy route `/api/zroky/*` with env-driven auth header injection; validated with `npm run lint` and `npm run build`.
